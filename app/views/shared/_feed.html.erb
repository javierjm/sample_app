<% if @feed_items.any? %>
  <ol class="microposts">
  <!-- this render will look for micropost partial since each member fo feed_items is of the kind micropost, so will look for the partial: app/views/microposts/_feed.html.erb -->
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>